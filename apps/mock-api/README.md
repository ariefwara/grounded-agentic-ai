# Mock API

Mock API mensimulasikan external data source dan external tools di luar sistem utama.

## Run

```bash
npm install
npm start
```

Default URL:

```text
http://localhost:3002
```

## Endpoints

- `GET /health`
- `POST /context`
- `POST /data/search`
- `POST /tools/execute`
