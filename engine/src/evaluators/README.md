# Evaluators

This folder is reserved for deeper evaluators that are shared across gates.

The first engine scaffold keeps simple deterministic checks inside `engine/gates`.
Move logic here when an evaluator becomes reusable, model-backed, or complex enough
to deserve its own contract.
